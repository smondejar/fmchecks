# Ultra-simple .htaccess for debugging
RewriteEngine On

# Log what Apache is doing (to environment variable)
RewriteRule .* - [E=REWRITE_TEST:1]

# Direct test - rewrite EVERYTHING to index.php (no conditions)
RewriteRule ^.*$ index.php [L,QSA]
